const EventDetails = ({ eventId }) => {
    return (
      <div>
        <h1>Event Details</h1>
        {/* Display event details based on eventId */}
      </div>
    );
  };
  
  export default EventDetails;
  